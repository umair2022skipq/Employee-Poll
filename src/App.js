import "./App.css";
import { AppBar, Toolbar, Typography } from "@mui/material";

const App = () => {
  return (
    <AppBar>
      <Toolbar>
        <Typography>Hello World</Typography>
      </Toolbar>
    </AppBar>
  );
};

export default App;
